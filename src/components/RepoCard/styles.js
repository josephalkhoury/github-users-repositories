import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
    card: {
        padding: '10px',
        height: '100%'
    },
    container:{
        height:'100%'
    }
}));