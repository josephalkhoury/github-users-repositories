import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
    container: {
        cursor: 'pointer',
        padding: '10px'
    },
    avatar:{
        borderRadius:'50%'
    }
}));